import EmBreve from '@/components/admin/EmBreve'

export default function AssociadosPage() {
  return (
    <EmBreve
      titulo="Associados"
      descricao="Cadastro completo dos filiados exibidos no site."
      permissao="associados.ver"
      previsto="A ficha completa do associado (fotos, contato, localização, horário) e o portal de autoatendimento chegam na fase seguinte."
    />
  )
}
